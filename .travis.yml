language: php
php:
  - 7.1
before_script:
  - composer install
  - composer dump-autoload
  - cp .env.example .env
  - php artisan jwt:secret
  - php artisan key:generate
  - php artisan vendor:publish
script: phpunit